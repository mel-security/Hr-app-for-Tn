-- ==============================================
-- HR CRÉDIT - Seed: Création du compte admin
-- Ce script est exécuté automatiquement au
-- premier démarrage de PostgreSQL.
-- ==============================================
-- Note: Le mot de passe est hashé avec bcrypt.
-- Le hash ci-dessous correspond à "admin123" (à changer!)
-- Utilisez le script seed-admin.sh pour générer un hash personnalisé.

-- Le vrai seeding se fait via l'API /api/auth/register
-- ou via le script seed-admin.sh après le premier démarrage.

-- Placeholder: le deploy.sh injectera le vrai admin ici.
